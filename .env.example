# OpenSync Environment Variables (Pocketbase Edition)
# Copy to .env.local and fill in your values

# Pocketbase URL
# Development: http://localhost:8090 (Vite proxies /api/* requests)
# Production: Your Pocketbase server URL (e.g., https://opensync.helmus.me)
VITE_POCKETBASE_URL=http://localhost:8090

# Optional: OpenAI for semantic search embeddings (deferred feature)
# OPENAI_API_KEY=sk-...
